* Execution: 
	$> 
	+ cd /workspace/app/ 
	+ mvn clean package 
		# java.lang.RuntimeException: Driver org.postgresql.Driver claims to not accept jdbcUrl, jdbc:h2:mem:testdb 
	+ mvn clean package --define skipTests 
		# [INFO] BUILD SUCCESS 
	+ ls -A target/
		# warehouse-api-1.0.0.jar 
	+ cd /workspace/practice/012226/ && mkdir Basic ; mkdir Advanced 
	# Basic:
	+ cd Basic/ 
	+ cp /workspace/app/target/*.jar . 
	+ touch Dockerfile ; touch .dockerignore ; touch docker-compose.yml 
	# Edit with VS Code in Win. 
	+ docker compose up -d 
	+ docker compose ps 
		# basic-app-1, basic-db-1 
	# FF: http://localhost:8080/api/products/health: Warehouse API is running! 
	+ docker compose down -v --rmi all 
	# Advanced: 
	+ cd /workspace/practice/012226/Advanced/ 
	+ cp ../Basic/.dockerignore . ; cp ../Basic/docker-compose.yml . 
	+ cp /workspace/app/pom.xml . ; cp -r /workspace/app/src/ . 
	+ touch Dockerfile 
	# Edit in VS Code copying over from solution. 
	+ docker compose up 
		# Failed to resolve maven:3.6-eclipse-temurin-11: not found 
	# Updated to maven:3.8-eclipse-temurin-11. 
	# FF: http://localhost:8080/api/products/health: Warehouse API is running! 


* Answers: 
	1.- $ java -jar app.jar 
	2.- $ mvn clean package 
	3.- $ mvn clean package -DskipTests 
	4.- target/ 
	5.- .dockerignore: 
		*.txt 
		target/
	6.- > 
		+ Bind Mount: 
			- Simply map a host directory to the container. 
			- Syntax: ./windows_folder:/workspace/share/ 
			- Deleting a file inside the container also removes it on the host. 
		+ Named Volume: 
			- Managed by Docker. 
			- Syntax: db_data:/var/lib/postgresql/data/ 
			- Survives container deletion. 
	7.- $ docker compose logs -f 	
		# Or just do `up` without `--detach` to see all logs. 
	8.- > 
		+ ENTRYPOINT: 
			- Command that always runs. 
			- Harder to override with --entrypoint. 
		+ CMD: 
			- Easy to override. 
		+ Both: 
			- Add args to ENTRYPOINT with CMD. 
	9.- > 
		+ FROM maven 
		+ eclipse-temurin:11-alpine-3.20 	# Auto-completed by VS Code. 
	10.- Ubuntu VM/physical: /var/lib/postgresql/16/data/ 
	11.- > 
		+ postgres. 
		+ postgres. 
		+ No default password, DB doesn't start if POSTGRES_PASSWORD not set. 
	12.- ??? 
	13.- $ git clone --branch system --single-branch --depth 1 <URL.git> 
	14.- $ docker stats 
	15.- > 
		+ Maven is a build and dependency management tool for Java projects. 
		+ Lifecycle: clean, compile, test, package, deploy. 
	16.- Project Object Model is the key file Maven needs to: 
		+ Manage plugins. 
		+ Define build process. 
	17.- ???

	# EXTRA: 
	18.- Validate/lint Dockerfile: 
		+ $ docker run --rm -i hadolint/hadolint < Dockerfile 
